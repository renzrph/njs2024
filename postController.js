const posts = [
  { id: 1, title: "Post  One" },
  { id: 2, title: "Post  Two" },
];

export const getPosts = () => posts;

// export {getPosts};

export const getPostsLength = () => posts.length;

// pag default
export default getPosts;
